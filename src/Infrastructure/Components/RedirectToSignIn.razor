@inject NavigationManager NavigationManager

@code {

	protected override void OnInitialized()
	{
		NavigationManager.NavigateTo($"/account/signin?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
	}

}